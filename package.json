{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "npx nuxt dev --no-warnings",
    "generate": "npx nuxt generate",
    "preview": "npx nuxt preview",
    "postinstall": "npx nuxt prepare",
    "homepage": "https://silvertree7622.github.io/flux/",
    "page": "gh-pages --dotfiles -d .output/public",
    "deploy": "npm run generate && npm run page"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.5.3",
    "@vueuse/core": "^11.1.0",
    "@vueuse/motion": "^2.2.6",
    "flowbite": "^2.5.1",
    "pinia": "^2.2.1",
    "vue": "latest"
  },
  "devDependencies": {
    "@nuxt/devtools": "^1.5.0",
    "@nuxtjs/tailwindcss": "^6.12.1",
    "fs": "^0.0.1-security",
    "gh-pages": "^6.1.1",
    "nuxt": "3.12"
  }
}
